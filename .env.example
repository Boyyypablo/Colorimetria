# PostgreSQL (local via docker compose)
DATABASE_URL="postgresql://colometria:colometria@localhost:5433/colometria?schema=public"

# Auth.js — gere com: openssl rand -base64 32
AUTH_SECRET="troque-por-um-segredo-longo"
AUTH_URL="http://localhost:3000"
# Com túnel (testes externos): rode npm run tunnel e troque AUTH_URL pela URL https://….trycloudflare.com
# (Auth.js também aceita o host da requisição via trustHost)

# Storage local de imagens (volume Docker em produção: /app/uploads)
UPLOAD_DIR="uploads"

# Simulação visual — catálogo de peças na análise (sem crédito de IA)
# Cloud (huggingface/gemini/fal) fica desligado até haver cota. Mock = overlay local.
VTO_PROVIDER="mock"
# Token: https://huggingface.co/settings/tokens
# Fine-grained com “Make calls to Inference Providers”
HF_TOKEN=""
# HF_VTO_MODEL="black-forest-labs/FLUX.1-Kontext-dev"

VTO_DAILY_LIMIT="5"
# E-mails sem limite diário (separados por vírgula)
VTO_UNLIMITED_EMAILS=""

# Seed: usuários demo só se DATABASE_URL for localhost (nunca produção)
# ALLOW_DEMO_SEED="false"

# Visão / colorimetria
FACE_DETECTOR="blazeface"
COLOR_PREDICTOR="rules"
PHOTO_RETENTION_DAYS="365"

# Consultora IA
# cards = plano gratuito (rubrica + cards) | ollama = local | gemini = nuvem paga
# Na Vercel, ollama vira cards automaticamente
CONSULTANT_AI="ollama"
OLLAMA_HOST="http://127.0.0.1:11434"
OLLAMA_MODEL="qwen2.5:7b"
OLLAMA_EMBED_MODEL="nomic-embed-text"
# Gemini (só se CONSULTANT_AI=gemini)
# GOOGLE_GENERATIVE_AI_API_KEY=""
# CONSULTANT_AI_MODEL="gemini-2.5-flash"

